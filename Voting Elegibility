#include<iostream>

using namespace std;
int main()
{
    int age;
    cout<<"Enter your age: ";
    cin>>age;
    if(age>=18)
    cout<<"You are elegible to vote";
    else
    cout<<"You are not elegible to vote";
    return 0;
}
